import { NewsComponent } from "./news.component";
import { NewsService } from '../news.service';

describe('Testin newcomponent', () => {
    let newsComponent;
    
    beforeEach(() =>{
        // newsComponent = new NewsComponent();
    })

    // it('get article should return artilces ', () => {
    //     const isOpen = newsComponent.article();
    //     expect(isOpen).toBe(NewsService);
    // });
})
